$(document).ready(function(){
    $('.multiitems').multipleSelect({
        placeholder:'Please select an item',
        multipleWidth: 500
    });
    $('.category_items').multipleSelect({
        placeholder:'Select Categories',
        multipleWidth: 500
    });
    $('.cycle_items').multipleSelect({
        placeholder:'Select Cycles',
        multipleWidth: 500
    });
    $('.phase_items').multipleSelect({
        placeholder:'Select Phases',
        multipleWidth: 500
    });
});


